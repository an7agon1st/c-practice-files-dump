#include <stdio.h>
#include <stdlib.h>

int main(int argc, char const *argv[])
{
	int n;
	double l,r,x;
	double p[20];
	scanf("%d %lf %lf %lf",&n, &l,&r,&x);

	int i=0;
	while(i<n){
		scanf("%d ",&p[i]);
	}
	return 0;
}